[theme]
base = "light"
primaryColor = "#006DB9"
backgroundColor = "#FFFFFF"
secondaryBackgroundColor = "#F8F9FA"
textColor = "#1A1A1A"

[server]
# Prevent "ValueError: Paths don't have the same drive" on Windows
fileWatcherType = "none"
runOnSave = false
